# SteamBridge Access Transformers (Forge 1.7.10)
# Intentionally empty: NetworkManager fields are accessed via ReflectionHelper
# (MCP/SRG names differ across 1.7.10 mapping sets; AT breaks deobf if wrong).
