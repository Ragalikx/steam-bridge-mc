# SteamBridge Access Transformers (Forge 1.8.9)
# Intentionally empty: NetworkManager fields are accessed via ReflectionHelper
# (MCP/SRG names differ across 1.8.9 mapping sets; AT breaks deobf if wrong).
