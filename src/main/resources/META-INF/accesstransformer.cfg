# SteamBridge Access Transformers (NeoForge 1.20.1)
#
# The 1.12.2 SRG field targets (field_151272_f / field_150746_g / field_150744_e)
# do not exist under 1.20.1 official mappings. The networking-stack access this mod
# needs (Connection.channel / Connection.address / ServerConnectionListener.connections)
# is now provided through accessor Mixins in steambridge.mixin.* instead of ATs.
#
# Intentionally empty. Add AT lines here only if a specific field/method cannot be
# reached via a @Mixin @Accessor.
