# SteamBridge Access Transformers (Forge 1.16.5)
#
# Intentionally empty. Networking access uses reflection / type-based field lookup
# (same approach as 1.19.2) rather than SRG AT names that break across mappings.
