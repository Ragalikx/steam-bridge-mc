org.gradle.jvmargs=-Xmx4G
org.gradle.daemon=false

# ---- Project coordinates ----
mod_id=steambridge
mod_name=Steam Bridge
mod_group=steambridge

# ---- Platform versions ----
minecraft_version=1.21.1
minecraft_version_range=[1.21.1,1.22)
# NeoForge 1.21.1 uses the new net.neoforged:neoforge coordinate (not the legacy :forge).
neo_version=21.1.234
neo_version_range=[21.1.0,)
loader_version_range=[4,)

# ---- Runtime deps ----
steamworks4j_version=1.10.0
# Pinned to the JNA version NeoForge 1.21.1 ships at runtime (compileOnly - NOT bundled).
# Verify on first build: if the game fails to launch with a module conflict, check
# NeoForge's own pom for net.java.dev.jna version and update this property.
jna_version=5.14.0

# ---- ADDED DURING MULTILOADER PORT ----

# the version of minecraft used for 'common', see https://projects.neoforged.net/neoforged/neoform
neo_form_version=1.21.1-20240808.144430

parchment_minecraft=1.21.1
parchment_version=2024.11.17

fabric_version=0.109.0+1.21.1
fabric_loader_version=0.16.9

neoforge_version=21.1.80
neoforge_loader_version_range=[4,)

mod_author=Ragalikx
license=MIT
credits=Ragalikx (author); Valve (Steam SDR); code-disaster (steamworks4j); java-native-access (JNA)
java_version=21