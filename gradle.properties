org.gradle.jvmargs=-Xmx4G
org.gradle.daemon=false

# ---- Project coordinates ----
mod_id=steambridge
mod_name=Steam Bridge
mod_group=steambridge
# ---- Platform versions ----
minecraft_version=1.20.1
# NeoForge 1.20.1 is published under the legacy "forge" coordinate + 47.1.x scheme.
neoforge_version=1.20.1-47.1.106

# ---- Runtime deps ----
steamworks4j_version=1.10.0
# Pinned to the exact JNA version the NeoForge 1.20.1 / FML 47.2.2 loader ships
# (compileOnly - NOT bundled, see build.gradle for why).
jna_version=5.12.1
